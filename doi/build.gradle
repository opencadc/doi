plugins {
    id 'war'
    id 'maven'
    id 'maven-publish'
    id 'com.jfrog.bintray' version '1.7.3'
}

repositories {
    jcenter()
    mavenLocal()
//    maven {
//        url = 'http://www.cadc-ccda.hia-iha.nrc-cnrc.gc.ca/m2repo'
//    }
    mavenCentral()
}

apply from: '../service.gradle'

sourceCompatibility = 1.7

group = 'ca.nrc.cadc'

version = '1006'

copy {
    from (System.getenv('A') + '/etc/') {
        include 'datacite.pass'
    }
    into 'src/main/resources'
}

war {
    archiveName = project.name + '##' + project.version + '.war'

    // Include the swagger-ui so that /doi provides DOI API documentation
    from(System.getenv('RPS') + '/resources/') {
        include 'swagger-ui/'
    }
}

dependencies {
    compile 'log4j:log4j:1.2.+'
    providedCompile 'javax.servlet:javax.servlet-api:3.1.+'
    
    compile 'org.opencadc:cadc-util:[1.2.5,)'
    runtime 'org.opencadc:cadc-log:[1.0,)'
    compile 'org.opencadc:cadc-rest:[1.1.1,)'
    compile 'org.opencadc:cadc-access-control-identity:1.+'
    compile 'org.opencadc:cadc-vosi:[1.2,2.0)'
    compile 'org.opencadc:cadc-vos:[1.1.2,)'

    runtime 'org.opencadc:cadc-registry:[1.4.1,)'

    testCompile 'junit:junit:[4.0,5.0)'
    intTestCompile 'org.opencadc:cadc-test-vosi:[1.0.3,)'
}
