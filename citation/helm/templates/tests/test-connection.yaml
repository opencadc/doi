apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "citation.fullname" . }}-test-connection"
  labels:
    {{- include "citation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "citation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
