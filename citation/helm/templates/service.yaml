apiVersion: v1
kind: Service
metadata:
  name: {{ include "citation.fullname" . }}
  labels:
    {{- include "citation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "citation.selectorLabels" . | nindent 4 }}
