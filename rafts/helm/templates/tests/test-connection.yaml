apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rafts.fullname" . }}-test-connection"
  labels:
    {{- include "rafts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rafts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
