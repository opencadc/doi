apiVersion: v1
kind: Service
metadata:
  name: {{ include "rafts.fullname" . }}
  labels:
    {{- include "rafts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rafts.selectorLabels" . | nindent 4 }}
